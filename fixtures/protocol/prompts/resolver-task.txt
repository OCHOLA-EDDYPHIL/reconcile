Compare stated intent, resource identities, dependencies, ownership, and recovery references. Preserve uncertainty. A proposed relation must quote an exact span from each supporting artifact and must not replace structured facts. Honor the request's disabled_relation_types field and do not infer from an artifact omitted by the selected ablation. Return exactly one JSON object that validates against the request's semantic_output_schema; do not add prose or fields outside that schema.
