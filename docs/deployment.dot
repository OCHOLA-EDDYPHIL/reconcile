digraph ReconcileDeployment {
  graph [
    bgcolor="#07111f",
    fontname="Liberation Sans",
    label=<<FONT POINT-SIZE="34"><B>Reconcile</B> — GOOGLE CLOUD TRUST TOPOLOGY</FONT><BR/><FONT POINT-SIZE="20">Operator, deployment, runtime, and public evidence authority stay separate.</FONT>>,
    labelloc="t",
    labeljust="l",
    fontcolor="#f8fafc",
    pad=0.42,
    rankdir=TB,
    ranksep=1.18,
    nodesep=0.58,
    newrank=true,
    ratio=fill,
    size="14.3,8.0!",
    splines=polyline,
    compound=true
  ];
  node [
    shape=box,
    style="rounded,filled",
    fontname="Liberation Sans",
    fontsize=20,
    fontcolor="#e2e8f0",
    color="#60a5fa",
    fillcolor="#13253d",
    margin="0.22,0.17",
    penwidth=1.7
  ];
  edge [
    fontname="Liberation Sans",
    fontsize=15,
    fontcolor="#cbd5e1",
    color="#64748b",
    arrowsize=0.80,
    penwidth=1.7
  ];

  operator [
    label="OPERATOR IDENTITY\nInvoke the API only",
    fillcolor="#172554",
    color="#818cf8"
  ];
  deployer [
    label="DEPLOYMENT IDENTITY\nProvision infrastructure only",
    fillcolor="#3f2d14",
    color="#fbbf24"
  ];

  subgraph cluster_core {
    label="OPERATIONAL CORE • SEPARATE SERVICE IDENTITIES";
    fontname="Liberation Sans";
    fontsize=18;
    fontcolor="#cbd5e1";
    color="#475569";
    style="rounded";
    margin=20;

    api [label="CLOUD RUN API\nAuthenticated entry\nDurable orchestration"];
    controller [
      label="CLOUD RUN CONTROLLER\nAdvisory planning\nEvidence admission and verification"
    ];
    proxy [
      label="CLOUD RUN FAULT PROXY\nPermit-gated mutation\nControlled acknowledgement loss",
      fillcolor="#172554",
      color="#818cf8"
    ];
    target [label="CLOUD RUN TARGET\nStaged release and traffic state"];
    sandbox [label="CLOUD RUN SANDBOX\nIsolated provider-shaped state"];

    { rank=same; api; controller; proxy; }
    { rank=same; target; sandbox; }
    api -> controller [label="investigate"];
    controller -> proxy [label="exact permit", color="#34d399"];
    proxy -> target [label="permitted action", color="#34d399"];
    controller -> sandbox [label="read", style=dashed, color="#38bdf8"];
    proxy -> sandbox [label="mutate", color="#34d399"];
  }

  subgraph cluster_data {
    label="MANAGED SERVICES • LEAST-PRIVILEGE ACCESS";
    fontname="Liberation Sans";
    fontsize=18;
    fontcolor="#cbd5e1";
    color="#475569";
    style="rounded";
    margin=20;

    vertex [
      label="VERTEX AI\nBounded advisory planning",
      shape=component,
      fillcolor="#2e1065",
      color="#c4b5fd"
    ];
    authority [
      label="FIRESTORE AUTHORITY\nRecovery state • permit claims\nEvent journal • delivery state",
      shape=cylinder,
      fillcolor="#082f49",
      color="#38bdf8"
    ];
    provider_state [
      label="PROVIDER STATE\nRelease and sandbox evidence",
      shape=cylinder,
      fillcolor="#082f49",
      color="#38bdf8"
    ];
    custody [
      label="PRIVATE CUSTODY\nOperator records and raw evidence",
      shape=folder,
      fillcolor="#3f2d14",
      color="#fbbf24"
    ];
    observability [
      label="CLOUD LOGGING + MONITORING\nStructured signals\nMetrics • alerts • dashboard",
      shape=component,
      fillcolor="#052e2b",
      color="#34d399"
    ];

    { rank=same; authority; vertex; provider_state; }
    { rank=same; custody; observability; }
  }

  subgraph cluster_public {
    label="PUBLIC EVIDENCE SURFACE • NO OPERATIONAL AUTHORITY";
    fontname="Liberation Sans";
    fontsize=18;
    fontcolor="#cbd5e1";
    color="#475569";
    style="rounded";
    margin=20;

    versioned [
      label="VERSIONED EVIDENCE\nSanitized immutable files",
      shape=folder,
      fillcolor="#3f2d14",
      color="#fbbf24"
    ];
    viewer [
      label="CLOUD RUN VIEWER\nStatic HTML and JSON\nRead-only routes",
      fillcolor="#1f2937",
      color="#94a3b8"
    ];
    public [label="PUBLIC READER\nBrowser or offline verifier"];

    versioned -> viewer [label="bundled at build time", style=dashed];
    viewer -> public [label="read-only response", dir=back];
  }

  { rank=same; operator; deployer; }
  operator -> api [label="authenticated invocation", color="#818cf8"];
  deployer -> api [label="provision only", style=dashed, color="#fbbf24"];
  controller -> vertex [label="advisory request", color="#a78bfa"];
  controller -> authority [label="authority state", color="#38bdf8"];
  controller -> provider_state [label="evidence reads", color="#38bdf8"];
  controller -> observability [label="correlated signals", style=dashed, color="#34d399"];
  proxy -> provider_state [label="permitted write", color="#34d399"];
  custody -> versioned [label="sanitized export", style=dashed, color="#fbbf24"];
}
