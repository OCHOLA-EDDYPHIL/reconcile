[project]
name = "reconcile"
version = "0.1.0"
description = "Evidence-backed reconciliation of ambiguous agent tool outcomes"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
reconcile = "reconcile.cli:main"
